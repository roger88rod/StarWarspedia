query Character($person_id: ID!) {
    person(id: $person_id) {
        id
        name
        eyeColor
        hairColor
        skinColor
        birthYear
        vehicleConnection {
            vehicles {
                name
            }
        }
    }
}